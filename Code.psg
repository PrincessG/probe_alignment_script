#!/usr/bin/env python
# encoding: utf-8

import sys
from Bio import SeqIO
for seq_record in SeqIO.parse("UCE_PROBES.fasta", "fasta"):
  sys.stderr.write(seq_record.id)
  sys.stderr.write(repr(seq_record.seq))  
  sys.stderr.write(%len(seq_record))
  
