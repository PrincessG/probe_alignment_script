#!/usr/bin/env python
# encoding: utf-8

import sys
from Bio import SeqIO

for seq_record in SeqIO.parse("UCE_PROBES.fasta", "fasta"):
  print(seq_record.id)
  print(repr(seq_record.seq))  
  print(len(seq_record))
  
#OutFileName= seq_record.id + '.fasta'
#WriteOutFile= True
# if WriteOutFile:
#  OutFile= open(OutFileName, 'w')
#  OutFile.write(seq_record.id + '\n')
# OutFile.write(repr(seq_record.seq + '\n'))
#  OutFile.write(len(seq_record + '\n'))
  
